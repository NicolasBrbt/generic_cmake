set (main_c_SOURCES "")
set (main_c_HEADERS"")

file(GLOB main_c_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c")
file(GLOB main_c_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/include/*.h")

add_executable(main_c ${main_c_SOURCES} ${main_c_HEADERS})

target_include_directories(main_c PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)

add_dependencies(main_c copy_headers)

add_dependencies(main_c copy_headers)
if(MSVC)
	target_compile_options(main_c PRIVATE /W4 /WX)
else()
	target_compile_options(main_c PRIVATE -Wall -Wextra -Wpedantic)
endif()
